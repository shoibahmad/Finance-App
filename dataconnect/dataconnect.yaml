specVersion: "v1"
serviceId: "finance"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finance-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
